<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.libre.mapper.UserRoleMapper">
    <!--分页查询用户角色-->
    <select id="pageQueryUserRole" resultType="com.libre.pojo.vo.UserRolePageVO">
        select user_role.id, user.username, role.role_name
        from tb_user_role user_role
        join tb_user user on user_role.user_id = user.id
        join tb_role role on user_role.role_id = role.id
        where user_role.is_delete = 0
        <if test="userRolePageDTO.roleId!=null">
            and user_role.role_id = #{userRolePageDTO.roleId}
        </if>
    </select>
</mapper>
