<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.libre.mapper.UserRoleMapper">
    <!--获取用户角色信息-->
    <select id="getUserRoles" resultType="com.libre.pojo.vo.RoleVO">
        select role.id, role.role_name
        from tb_user_role user_role
                 join tb_role role on user_role.role_id = role.id
        where user_role.is_delete = 0
          and user_role.user_id = #{userId}
    </select>
</mapper>
