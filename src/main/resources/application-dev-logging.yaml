# 日志配置
logging:
  file:
    name: logs/app.log  # 会在项目根目录下生成 logs/app.log
  level:
    root: warn # 框架日志级别
    com.libre: debug # 业务日志级别
  pattern:
    # 控制台日志格式（开发用，更简洁）
    console: "%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] [%logger{36}] - %msg%n"
    # 文件日志格式（保留完整日期时间）
    file: "%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] [%logger{36}] - %msg%n"